#pragma once

enum decodeConfig
{
    default_ = 1,
    allDisable,
    allEnable
};

enum interleaved2of5ParityProcess
{
    mode10 = 1,
    none
};

enum industrial25ParityProcess
{
    mode10 = 1,
    none
};

enum matrix2of5ParityProcess
{
    mode10 = 1,
    none
};

enum code11ParityProcess {
    bit1 = 1,
    bit2
};

enum MSIParityProcess {
    singleMod10,
    doubleMod10
};

